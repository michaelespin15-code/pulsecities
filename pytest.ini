[pytest]
markers =
    integration: marks tests as requiring a live PostgreSQL database (deselect with -m "not integration")
    needs_data: marks tests that need a database holding records, not just the schema
